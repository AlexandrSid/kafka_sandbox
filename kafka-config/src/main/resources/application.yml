spring:
  application:
    name: kafka-config-service
  kafka:
#    bootstrap-servers: localhost:9092
    bootstrap-servers: localhost:9092,localhost:9192,localhost:9292 # На стадии 4
    topics:
      - name: goal_completed
        partitions: 3
        replicas: 3
      - name: user_subscribe
        partitions: 3
        replicas: 3

logging.level.org.apache.kafka.clients.admin: INFO