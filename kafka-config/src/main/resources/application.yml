spring:
  application:
    name: kafka-config-service
  kafka:
    bootstrap-servers: localhost:9092
#    bootstrap-servers: localhost:9092,localhost:9094,localhost:9096 # На стадии 4
logging.level.org.apache.kafka.clients.admin: INFO